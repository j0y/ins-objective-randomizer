"""Generators: a map spec becomes geometry.

Street grid and block subdivision, buildings extruded from a room-and-corridor
spec, door and window cuts, prop placement from the asset catalogue, then the
gameplay entities - spawns, ins_objective, caches, spawn zones.
"""
