"""Software renderer: look at the geometry from inside it.

Flat-shaded perspective views from player eye height at sampled standpoints.
Crude next to the real engine, but enough to judge whether a room reads as a
room and whether a landmark is visible from where it needs to be.
"""
