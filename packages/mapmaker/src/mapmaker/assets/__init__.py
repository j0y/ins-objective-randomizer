"""What the game ships: read the kit so the generator can place from it.

VPK archives, .mdl headers (bounding box, $staticprop flag), .phy collision
hulls, .vtf textures, and the catalogue that indexes them by name so a
generator can ask for "a five-storey Pripyat apartment facade" and get models
that actually exist in the install.
"""
